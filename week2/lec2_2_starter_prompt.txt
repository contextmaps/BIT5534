I want to build a Python script that runs a one-way ANOVA on any CSV or Excel file with two columns, where the first column is a continuous response and the second is a categorical factor. I run it by double clicking, so it should let me choose any input file in that format. It should start with the F test and stop if it fails. Then it should run Levene's test for unequal variances. If the variances are alike, it should run the Tukey HSD test. If they differ, it should run the Welch test. If all the tests pass, it should also report the connecting letters. Ask me one question at a time about whatever I have left unsettled, at most five, then write the prompt.
